git --version
git init -b main
git config -global
git config - global user.name
git config -global user.password
git log
git commit
git status
git commit -a -m "
git 
git diff -- staged
git rm cached 
git clone



//to add in remote 
git remote add origin ssh-key
git remote -version
git push -u origin main

tagging is used to provide version number
git tag -a v1.0 -m "

git show to ger inforamtion of the tag